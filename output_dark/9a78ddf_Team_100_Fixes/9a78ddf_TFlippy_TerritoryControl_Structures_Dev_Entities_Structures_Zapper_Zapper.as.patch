diff --git a/TFlippy_TerritoryControl_Structures_Dev/Entities/Structures/Zapper/Zapper.as b/TFlippy_TerritoryControl_Structures_Dev/Entities/Structures/Zapper/Zapper.as
index 472e933..aec1ee6 100644
--- a/TFlippy_TerritoryControl_Structures_Dev/Entities/Structures/Zapper/Zapper.as
+++ b/TFlippy_TerritoryControl_Structures_Dev/Entities/Structures/Zapper/Zapper.as
@@ -56,7 +56,7 @@
 
 bool isInventoryAccessible(CBlob@ this, CBlob@ forBlob)
 {
-	if (this.getTeamNum() == forBlob.getTeamNum() || (this.getTeamNum() > 100 && this.getTeamNum() < 200))
+	if (this.getTeamNum() == forBlob.getTeamNum() || (this.getTeamNum() >= 100 && this.getTeamNum() < 200))
 	{
 		CBlob@ carried = forBlob.getCarriedBlob();
 		return (carried is null ? true : carried.getName() == "mat_battery");
