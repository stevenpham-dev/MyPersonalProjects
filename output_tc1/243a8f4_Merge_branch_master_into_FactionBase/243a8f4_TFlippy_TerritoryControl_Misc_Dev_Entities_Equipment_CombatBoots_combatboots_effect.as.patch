diff --git a/TFlippy_TerritoryControl_Misc_Dev/Entities/Equipment/CombatBoots/combatboots_effect.as b/TFlippy_TerritoryControl_Misc_Dev/Entities/Equipment/CombatBoots/combatboots_effect.as
index 791ffa9..7bdc0a0 100644
--- a/TFlippy_TerritoryControl_Misc_Dev/Entities/Equipment/CombatBoots/combatboots_effect.as
+++ b/TFlippy_TerritoryControl_Misc_Dev/Entities/Equipment/CombatBoots/combatboots_effect.as
@@ -2,30 +2,20 @@
 
 void onTick(CBlob@ this)
 {
-    if(this.get_string("reload_script") == "combatboots")
+    if (this.get_string("reload_script") == "combatboots")
         this.set_string("reload_script", "");
     
     RunnerMoveVars@ moveVars;
-    if(this.get("moveVars", @moveVars))
+    if (this.get("moveVars", @moveVars))
     {
         moveVars.walkFactor *= 1.2f;
     }
     
-    if(this.get_f32("cb_health") >= 10.0f)
+    if (this.get_f32("combatboots_health") >= 10.0f)
     {
         this.getSprite().PlaySound("ricochet_" + XORRandom(3));
         this.set_string("equipment_boots", "");
-        this.set_f32("cb_health", 0.0f);
+        this.set_f32("combatboots_health", 9.9f);
         this.RemoveScript("combatboots_effect.as");
     }
-}
-
-void onDie(CBlob@ this)
-{
-    if (isServer())
-    {
-        CBlob@ item = server_CreateBlob("combatboots", this.getTeamNum(), this.getPosition());
-        if (item !is null) item.set_f32("health", this.get_f32("cb_health"));
-    }
-    this.RemoveScript("combatboots_effect.as");//
 }
\ No newline at end of file
